import java.util.Scanner;

public class EnrolTest {
	public static void main(String[] args) {
	Enrol enrol1 = new Enrol();
	Enrol enrol2 = new Enrol();

	System.put.printf("%s has a serial number: %d and account balance: %.2f", enrol1.getName(), enrol1.getSerialNo(), enrol1.getAccountBal());
	System.put.printf("%s has a serial number: %d and account balance: %.2f", enrol2.getName(), enrol2.getSerialNo(), enrol2.getAccountBal());
	
	Scanner scanner = new Scanner(System.in);

	System.out.print("Enter enrol1 serial number: ");
	int serialNo = input.nextInt();
	System.out.print();

	}
}

